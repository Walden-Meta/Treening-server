# treening.cc 全局限流：按客户端 IP（$binary_remote_addr 为 nginx 直连 IP）
limit_req_zone $binary_remote_addr zone=treening_auth:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=treening_api:10m rate=240r/m;
limit_req_status 429;
