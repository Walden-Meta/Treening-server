# 前端阶段优化基线（2026-08-04）

本文件记录阶段性 UI 优化开始前的功能与布局基线。后续阶段必须至少保持这里列出的功能通过，并用同一组视口复查布局。

## 功能基线

- `node --check src/treening/static/js/tree.js`：通过。
- `node --test tests/fold-state.test.js`：4/4 通过。
- 折叠解析器覆盖：祖先折叠接管、后代折叠意图保留、互不相交牌堆、无效根过滤。
- 页面无横向滚动条：1512×982、1024×768、390×844 均通过。

## 视觉基线

| 视口 | H1 | 上下文条 | 画布 | 输入区 | 观察 |
| --- | ---: | --- | --- | --- | --- |
| 1512×982 | 46px | 771×38 | 771×574 | 771×178 | 三栏完整显示；顶部品牌区较高；完整树缩放后节点偏小 |
| 1024×768 | 34px | 961×54 | 961×344 | 961×178 | 两侧栏改为抽屉；画布纵向空间偏紧 |
| 390×844 | 30px | 345×68 | 345×557 | 345×180 | 画布优先；输入区首屏不可见，需要继续下滑 |

## 后续阶段的硬性回归边界

1. 不得破坏节点新增、选中、拖动、缩放、折叠、展开、隐藏与恢复。
2. 祖先折叠后，后代折叠意图必须保留；重新展开祖先后，后代仍保持折叠。
3. 三档视口均不得产生横向滚动条。
4. 1024px 以下的历史主题与节点阅读必须仍可通过抽屉访问。
5. 移动端输入区应比当前基线更容易触达，且不得遮挡无法恢复的画布操作。
